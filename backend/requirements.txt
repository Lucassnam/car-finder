# Car Finder — Python backend dependencies
# Install:  pip install -r backend/requirements.txt
# (the setup script does this for you inside backend/.venv)

# ── Web API ──────────────────────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6

# ── Scraping ─────────────────────────────────────────────
httpx>=0.27
selectolax>=0.3.21        # fast HTML parsing
beautifulsoup4>=4.12
lxml>=5.3
playwright>=1.48          # JS-heavy / anti-bot fallback (browsers installed separately)
feedparser>=6.0           # Craigslist RSS new-listing polling
tenacity>=9.0             # retry/backoff for polite scraping

# ── Database / ORM (PostgreSQL + PostGIS) ────────────────
SQLAlchemy>=2.0
alembic>=1.13             # migrations
psycopg[binary]>=3.2      # Postgres driver
GeoAlchemy2>=0.15         # PostGIS geometry types for the map

# ── Local AI (Ollama client) ─────────────────────────────
ollama>=0.3

# ── Deal-rating / sold-comps valuation model ─────────────
scikit-learn>=1.5
pandas>=2.2
numpy>=2.0

# ── Image handling (photo dedup + vision) ────────────────
pillow>=11.0
ImageHash>=4.3            # perceptual hashing for cross-post dedup

# ── Scheduling ───────────────────────────────────────────
APScheduler>=3.10

# ── Logging / utils ──────────────────────────────────────
loguru>=0.7
python-dotenv>=1.0
