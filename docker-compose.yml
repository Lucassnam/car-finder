services:
  db:
    image: postgis/postgis:16-3.4
    container_name: carfinder-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-carfinder}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-carfinder}
      POSTGRES_DB: ${POSTGRES_DB:-carfinder}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-carfinder}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
